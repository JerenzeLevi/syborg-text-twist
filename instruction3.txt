🎯 CORE DESIGN DIRECTION (Lock this first)

You’re mixing 3 things:

🔵 Tech / Cyber (Syborg identity)
🌊 Frutiger Aero (glass, soft gradients, bubbles)
⚡ 2000s OS UI nostalgia (Windows XP / Vista feel)

👉 The issue right now:

Colors are not unified (orange breaks immersion)
Background is flat
Buttons are low contrast + tiring
UI lacks depth hierarchy
🎨 1. SYBORG COLOR SYSTEM (Replace everything with this)

Use a tight palette — this is what makes it premium:

🔷 Primary (Syborg Core)
#0B1F3A – Deep navy (main background)
#123A6B – Panel base
#1F6FEB – Accent blue (interactive)
#38BDF8 – Glow / highlight
🌊 Aero Layer
rgba(255,255,255,0.15) – Glass
rgba(255,255,255,0.35) – Borders
rgba(56,189,248,0.25) – Glow tint
❌ REMOVE:
Orange buttons → replace with cyan/blue glow
Harsh white → soften to cool blue-white
🌌 2. BACKGROUND (THIS IS WHERE YOU WIN)

You said:

“bg should never be static blue”

Correct. Here's your premium direction:

💡 Option A — Animated Grid + Glow (RECOMMENDED)
background: radial-gradient(circle at 30% 30%, #1f6feb33, transparent 60%),
            radial-gradient(circle at 70% 70%, #38bdf833, transparent 60%),
            #0b1f3a;

Then overlay:

background-image: linear-gradient(rgba(255,255,255,0.05) 1px, transparent 1px),
                  linear-gradient(90deg, rgba(255,255,255,0.05) 1px, transparent 1px);
background-size: 40px 40px;
animation: gridMove 20s linear infinite;

👉 This gives:

Hacker vibe ✅
Aero softness ✅
Not static ✅
🪟 3. MAIN PANEL (Frutiger Aero Glass)

Your current panel is too flat.

Replace with:
background: rgba(255, 255, 255, 0.12);
backdrop-filter: blur(20px);
border: 1px solid rgba(255,255,255,0.25);
border-radius: 14px;
box-shadow:
  0 10px 40px rgba(0,0,0,0.4),
  inset 0 1px rgba(255,255,255,0.3);

👉 This creates:

Depth
Soft premium feel
Classic aero glass
🔤 4. LETTER BUTTONS (MAJOR FIX)

Current issue:

Orange = off-theme ❌
Hard to see ❌
Eye strain ❌
💎 New Style (Syborg Aero Buttons)
.letter {
  background: linear-gradient(
    180deg,
    rgba(56,189,248,0.9),
    rgba(31,111,235,0.9)
  );
  border-radius: 10px;
  color: white;
  font-weight: bold;
  box-shadow:
    0 4px 12px rgba(31,111,235,0.6),
    inset 0 1px rgba(255,255,255,0.5);
  transition: all 0.2s ease;
}

.letter:hover {
  transform: translateY(-2px) scale(1.05);
  box-shadow:
    0 6px 20px rgba(56,189,248,0.8);
}

👉 Result:

Still playful
Now premium
Matches Syborg identity
🫧 5. BUBBLE WORD SLOTS (FIX VISIBILITY)

You’re right—they’re:

“hard to see and painful”

Fix:
Increase contrast
Add inner glow
Add subtle pulse
.word-slot {
  background: rgba(255,255,255,0.18);
  border: 1px solid rgba(255,255,255,0.3);
  backdrop-filter: blur(10px);
  box-shadow:
    inset 0 0 10px rgba(56,189,248,0.3),
    0 0 6px rgba(56,189,248,0.2);
}

👉 Optional animation:

animation: pulseGlow 2s infinite ease-in-out;
🎮 6. BUTTONS (Enter / Back / Clear / Twist)

Make them icon + glow buttons, not plain UI.

.control-btn {
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(56,189,248,0.4);
  color: #38BDF8;
  border-radius: 8px;
  backdrop-filter: blur(10px);
}

.control-btn:hover {
  background: rgba(56,189,248,0.2);
  box-shadow: 0 0 12px rgba(56,189,248,0.5);
}
🧠 7. TYPOGRAPHY (CRUCIAL FOR PREMIUM FEEL)

Use:

Orbitron → headings (tech vibe)
Inter / Segoe UI → UI text (clean + readable)
font-family: 'Orbitron', sans-serif; /* title */
font-family: 'Inter', sans-serif;    /* UI */
⚡ 8. MICRO-INTERACTIONS (THIS MAKES IT FEEL EXPENSIVE)

Add:

Key press glow ripple
Correct word → flash cyan
Wrong word → subtle red shake (very soft)
🧩 9. FINAL UI IDENTITY SUMMARY

Your game should feel like:

🧊 A futuristic Windows Vista panel
⚡ running inside a hacker OS
🌊 with soft floating aero glass elements

🔥 HONEST FEEDBACK (IMPORTANT)

Right now:

UI = functional + nostalgic
Not yet = memorable or premium

After these changes:

It becomes brandable
It becomes “Syborg-level”